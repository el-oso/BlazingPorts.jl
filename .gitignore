# Julia
Manifest.toml
*.jl.cov
*.jl.*.cov
*.jl.mem

# Documenter / Node
docs/build/
**/.documenter/
node_modules/

# Rust comparison-crate build artifacts
**/target/
Cargo.lock

# Compiled shared libs (e.g. bench/mkl_amd/fakeintel.so — keep the .c source)
*.so
*.so.*

# Generated probe plots (docs/assets/*.png) and capped sample data (bench/results/*.json)
# are tracked so plots can be regenerated without re-running benchmarks (see Harness.replot).

.DS_Store
.claude/hooks/.src-hash
